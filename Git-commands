clone all repo with all branches:
    -git clone --bare <source-code-clone-link> 
    after cloning go to sub folder and execute: 
    -git push --mirror <target-code-clone-link> 
change upstream:    
    -git push --set-upstream origin <branchThatIwant>
https git repository "SSL certificate problem: self signed certificate in certificate chain":
    -git config --global http.sslVerify false
change/edit git remote repository address:
    -git remote -v //find all your remote addresses
    -git remote set-url origin https://USERNAME:password@gitserveraddress
git edit post buffers:
    -git config --global http.postBuffer 2097152000
    -git config --global https.postBuffer 2097152000
sort commits by date:
    git log --pretty="%h - %s" --author=gitster --since="2008-10-01" \   --before="2008-11-01" --no-merges -- t/
    git log --pretty=format:"%ce" --since="2017-09-01" --before="2017-10-31">D:\rally\commits.txt
error: failed to push some refs to [remote repo]:
    -git pull --allow-unrelated-histories
git config --list --show-origin
